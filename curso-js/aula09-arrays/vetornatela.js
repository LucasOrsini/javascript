let vetores =[8, 1, 7, 4, 2, 9]

console.log(vetores) // Mostra o vetor completo

for (let pos = 0; pos < vetores.length; pos++) { // Percorre o vetor
    console.log(`A posição ${pos} tem o valor ${vetores[pos]}`)
}